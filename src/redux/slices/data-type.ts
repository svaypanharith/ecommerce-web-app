

export interface StorageResponse {
    id: string;
    name: string;
    capacity: number;
    description: string;
    status: string;
}

export interface ColorsResponse {
    id: string;
    name: string;
    code: string;
}